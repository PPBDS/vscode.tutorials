---
title: Websites 2
author: David Kane
tutorial:
  id: "10-websites-2"
output:
  learnr::tutorial:
    progressive: yes
    'allow_skip:': yes
runtime: shiny_prerendered
description: 'A second tutorial about Quarto websites. Builds a site with multi-page navigation and drop-down menus, then refactors analysis code into scripts that save results as RDS and PNG files.'
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(echo = FALSE)
knitr::opts_chunk$set(out.width = '90%')
options(tutorial.exercise.timelimit = 240, 
        tutorial.storage = "local") 

library(learnr)
library(tutorial.helpers)
library(knitr)
library(tidyverse)
```



```{r info-section, child = system.file("child_documents/info_section.Rmd", package = "tutorial.helpers")}
```

<!-- For later websites, show how to add a link to the website to the GitHub repo. -->

## Introduction
### 

This tutorial practices creating and organizing [Quarto websites](https://quarto.org/docs/websites/) using the [Quarto CLI](https://quarto.org/docs/get-started/) (command line interface). You will learn some new skills: adding a visualization, creating new pages, building navigation menus, and --- most importantly --- organizing a site like a real project, with analysis code split into scripts that save their results as RDS and PNG files. Some material is from [*R for Data Science (2e)*](https://r4ds.hadley.nz/) by Hadley Wickham, Mine Çetinkaya-Rundel, and Garrett Grolemund.

### 

You should be doing this tutorial in a repo named `websites-2`. If you are not, create one and connect to it, as you learned in earlier tutorials.

## The website
### 

Let's create a website with a beautiful plot of the data from the `billboard` tibble from the **tidyr** package.

### Exercise 1

Copy/paste the URL for your `websites-2` repository on GitHub.

```{r the-website-1}
question_text(NULL,
	answer(NULL, correct = TRUE),
	allow_retry = TRUE,
	try_again_button = "Edit Answer",
	incorrect = NULL,
	rows = 3)
```

### 

```
https://github.com/ppbds-student/websites-2
```

###

Each website gets its own GitHub repo. This keeps projects independent: separate commit histories, separate collaborators, and separate deployment settings. It also means you can delete or archive a project without affecting anything else. This is good practice for any data science project, not just websites.

### Exercise 2

In the Terminal, run `quarto create project website .`.  Use "Websites 2" as the title, if asked.

CP/CR.

```{r the-websites-2}
question_text(NULL,
	answer(NULL, correct = TRUE),
	allow_retry = TRUE,
	try_again_button = "Edit Answer",
	incorrect = NULL,
	rows = 3)
```

### 

````
websites-2 $ quarto create project website .
? Title (website) › Websites 2
Creating project at /workspaces/websites-2:
  - Created _quarto.yml
  - Created .gitignore
  - Created index.qmd
  - Created about.qmd
  - Created styles.css
websites-2 $ 
````

We are moving faster than in the Websites 1 tutorial because you are already familiar with these steps. Later, we will also learn how to save analysis results to files so the site does not re-run all its code on every render.

### Exercise 3

Add `_site` to the `.gitignore` files. Save it.

In the Terminal, run `cat .gitignore`. CP/CR.

```{r the-website-3}
question_text(NULL,
	answer(NULL, correct = TRUE),
	allow_retry = TRUE,
	try_again_button = "Edit Answer",
	incorrect = NULL,
	rows = 3)
```

### 

````
websites-2 $ cat .gitignore
/.quarto/
**/*.quarto_ipynb
_site
websites-2 $ 
````

Keeping track of all the different files and understanding how they work together is the central skill in creating larger projects.

### Exercise 4

In `index.qmd`, delete everything outside the YAML header. Change `title` to `"Billboard Graphic"`. 

In the Terminal, run `cat index.qmd`. CP/CR.

```{r the-website-4}
question_text(NULL,
    answer(NULL, correct = TRUE),
    allow_retry = TRUE,
    try_again_button = "Edit Answer",
    incorrect = NULL,
    rows = 3)
```

### 

````
websites-2 $ cat index.qmd 
---
title: "Billboard Graphic"
---
websites-2 $ 
````

Because this is a Quarto website project, the format for all the `qmd` files is `html` by default. So the YAML header does not need to specify that explicitly. 

### Exercise 5

Create a new code chunk in `index.qmd` which loads the **tidyverse** package, with `#| message: false`. Save the file.

In the R Terminal, run:

````
show_file("index.qmd", chunk = "Last")
````

CP/CR. (Did you remember to `library(tutorial.helpers)` in the R Terminal so that this function is available?)

```{r the-website-5}
question_text(NULL,
	answer(NULL, correct = TRUE),
	allow_retry = TRUE,
	try_again_button = "Edit Answer",
	incorrect = NULL,
	rows = 3)
```

###

````
#| message: false
library(tidyverse)
````

###

Without `#| message: false`, the tidyverse startup messages would appear right on your home page. Chunk options are how a Quarto website keeps its pages clean.

### Exercise 6

From the Terminal, run `quarto preview`. 

Copy and paste the home page, leaving out the menu items.

```{r the-website-6}
question_text(NULL,
    answer(NULL, correct = TRUE),
    allow_retry = TRUE,
    try_again_button = "Edit Answer",
    incorrect = NULL,
    rows = 10)
```

### 

````
Websites 2

Billboard Graphic
library(tidyverse)
````

The code is echoed because we have not turned off echoing in the YAML header yet. Do that now with:

````
execute:
  echo: false
````  

Note how changing `index.qmd` causes the `index.html` to be re-rendered, leading to a new homepage display in your browser. Seeing fixes implemented automatically is why many people like working in *preview* mode.

### Exercise 7

Create another code chunk. Create a cool-looking plot using the `billboard` tibble from the **tidyr** package and `ggplot()`. Make sure you understand what the plot is showing. Work with AI to solve any errors, improve the code, make the graphic better and so on.

You now know two reasonable ways to work with AI, and either is fine here. You can ask an AI in a chat window --- Grok, ChatGPT, Claude, and so on --- and copy/paste the code it gives you into your chunk by hand, the way we did in the earlier tutorials. Or you can let an AI command-line assistant like `agy` read and edit `index.qmd` for you directly, the way you learned in the Antigravity tutorial. Use whichever you prefer.

`Cmd/Ctrl + Shift + K` to ensure that the plot looks good. If it looks bad, or there are some weird messages/warnings, use the AI to fix things.

In the R Terminal, run:

````
show_file("index.qmd", chunk = "Last")
````

CP/CR.

```{r the-website-7}
question_text(NULL,
	answer(NULL, correct = TRUE),
	allow_retry = TRUE,
	try_again_button = "Edit Answer",
	incorrect = NULL,
	rows = 3)
```

### 

What [Grok gave me](https://grok.com/chat/4cc49023-df73-4d2c-ad16-49cb52be7559) after 20 minutes of back-and-forth conversation:

````
data("billboard", package = "tidyr")

# billboard variables:
# - artist: name of the artist
# - track: song title
# - date.entered: date the song entered the Billboard Top 100
# - wk1 through wk76: weekly rankings from first week on chart, lower is better

# Step 1: Calculate week offset and find ranks in early July 2000 (approx. week 26)
billboard <- billboard |> 
  mutate(date.entered = as.Date(date.entered),
         week_offset = as.numeric(difftime(date.entered, as.Date("2000-01-01"), units = "weeks")))

# Approximate July 1, 2000 (week 26 from Jan 1)
jul_week <- 26

# Find the rank of each song around July 1, 2000
billboard_jul <- billboard |> 
  filter(date.entered <= as.Date("2000-07-01")) |> 
  mutate(wk_idx = jul_week - week_offset,
         wk_idx = if_else(wk_idx >= 1 & wk_idx <= 76, wk_idx, NA_real_),
         jul_rank = NA_real_)

# Compute jul_rank by extracting values from wk columns
for (i in seq_len(nrow(billboard_jul))) {
  wk_idx <- billboard_jul$wk_idx[i]
  if (!is.na(wk_idx)) {
    col_name <- paste0("wk", wk_idx)
    billboard_jul$jul_rank[i] <- as.numeric(billboard_jul[[col_name]][i])
  }
}

# Select top 5 songs by jul_rank
top_5_songs <- billboard_jul |> 
  filter(!is.na(jul_rank)) |> 
  arrange(jul_rank) |> 
  slice(1:5) |> 
  pull(track)

# Step 2: Filter data to only the top 5 songs for 2000
billboard_filtered <- billboard |> 
  filter(track %in% top_5_songs)

# Step 3: Reshape data for plotting, limiting to 2000 (weeks 0 to 52)
top_songs <- billboard_filtered |> 
  select(artist, track, wk1:wk76) |> 
  pivot_longer(cols = starts_with("wk"),
               names_to = "week_num",
               values_to = "rank") |> 
  mutate(week_num = as.numeric(gsub("wk", "", week_num))) |> 
  left_join(billboard_filtered |> select(track, week_offset), by = "track") |> 
  mutate(week_num = week_num + week_offset) |> 
  filter(week_num >= 0 & week_num <= 52) |> 
  filter(!is.na(rank)) |> 
  # Create a new column for the legend: "Song (Artist)"
  mutate(song_artist = paste0(track, " (", artist, ")"))

# Step 4: Plot
month_breaks <- seq(0, 48, by = 4)  # 13 breaks: 0 to 48
month_labels <- c("Jan", "Feb", "Mar", "Apr", "May", "Jun", 
                 "Jul", "Aug", "Sep", "Oct", "Nov", "Dec", "Jan")

ggplot(top_songs, aes(x = week_num, y = rank, color = song_artist)) +
  geom_line(linewidth = 1.2, alpha = 0.8) +
  geom_point(size = 2) +
  scale_y_reverse(limits = c(100, 1)) +
  scale_x_continuous(breaks = month_breaks, labels = month_labels) +
  scale_color_manual(values = c("#FF6B6B", "#4ECDC4", "#45B7D1", "#96CEB4", "#FFEEAD")) +
  labs(title = "Billboard Top 100: Top 5 Songs in Jul 2000",
       subtitle = "Tracking rank changes from Jan to Dec 2000",
       x = "Month",
       y = "Chart Position",
       color = "Song (Artist)") +
  theme_minimal() +
  theme(
    plot.title = element_text(size = 16, face = "bold", hjust = 0.5),
    plot.subtitle = element_text(size = 12, hjust = 0.5),
    legend.position = "bottom",
    legend.title = element_text(face = "bold"),
    panel.grid.minor = element_blank(),
    panel.grid.major = element_line(color = "grey90"),
    plot.background = element_rect(fill = "#F5F5F5", color = NA)
  ) +
  guides(color = guide_legend(nrow = 2))
````

###

As AI gets better, less iteration will be necessary. But you still need to be *intentional*, to demonstrate *agency* and *understanding*. You must be able to explain what the plot means. You don't have to *write* the code, but you should be able to *read* the code, perhaps after asking the AI for help.

### Exercise 8

There are two common modes for working with larger projects. First, as here, we work with an individual file, like `index.qmd`, and ensure that it is working as we want. Second, as before, we work with the project as a whole, using commands like `quarto render`, which render all the files in the project. The former approach is quickest and is how we spend most of our time. But, at the end of the day, we need to build the entire project.

Go to the Source Control pane. Commit and push your new changes to GitHub. Include a descriptive commit message, e.g. "Added plot to home page".

In the Terminal, run `quarto publish gh-pages`. You may need to hit `Enter` once.

Copy/paste the URL below:

```{r the-website-8}
question_text(NULL,
    answer(NULL, correct = TRUE),
    allow_retry = TRUE,
    try_again_button = "Edit Answer",
    incorrect = NULL,
    rows = 3)
```

### 

Your answer should look like this, but with your username instead of mine.

````
https://ppbds-student.github.io/websites-2/
````

Keep track of the three versions of the website. The QMD files in the project directory are the source — always make changes here. The rendered HTML files in `_site` are what you preview locally; never edit them directly. The published website on GitHub is what the world sees.

### Exercise 9

Open `about.qmd `. Delete all the current text, except for the YAML header. Ensure that the `title` is `"About"`. Add `## About Me` as the first (and only) line of text. Render the QMD. 

In the R Terminal, run:

````
show_file("about.qmd")
````

CP/CR.

```{r the-website-9}
question_text(NULL,
    answer(NULL, correct = TRUE),
    allow_retry = TRUE,
    try_again_button = "Edit Answer",
    incorrect = NULL,
    rows = 3)
```

### 

````
---
title: "About"
---

## About Me
````

###

Hash signs in front of text make the text into a header. The organization of headers is determined by the number of hash signs. Fewer hash signs make higher level headers, i.e. bigger text. More hash signs make lower level headers, i.e. smaller text. 

### Exercise 10

Write a one sentence description of yourself. Write it below the header. Include your name and school, either the one you attend or one from which you have graduated. Write until the end of the line, i.e. do not use the `Enter` key to wrap text. When you are finished, render the QMD. Example:

````
My name is David Kane, and I graduated from Williams College.
````

In the R Terminal, run:

````
show_file("about.qmd")
````

CP/CR.

```{r the-website-10}
question_text(NULL,
    answer(NULL, correct = TRUE),
    allow_retry = TRUE,
    try_again_button = "Edit Answer",
    incorrect = NULL,
    rows = 3)
```

### 

````
---
title: "About"
---

## About Me

My name is David Kane, and I graduated from Williams College.
````

###

You can have a Quarto document without a YAML header, if you like. You can also have an empty YAML header. But, it will often be the case that we want all the files in a project to have similar YAML headers in order to create a consistent look-and-feel.

### Exercise 11

Let's bold your name and italicize your school's name. In the description you have already written, surround your name with two asterisks, `**`, on both sides. Let's also put a single asterisk, `*`, around both sides of your school. Example:

````
My name is **David Kane**, and I graduated from *Williams College*.
````

`Cmd/Ctrl + Shift + K`

In the R Terminal, run:

````
show_file("about.qmd")
````

CP/CR.

```{r the-website-11}
question_text(NULL,
    answer(NULL, correct = TRUE),
    allow_retry = TRUE,
    try_again_button = "Edit Answer",
    incorrect = NULL,
    rows = 6)
```

### 

````
---
title: "About"
---

## About Me

My name is **David Kane**, and I graduated from *Williams College*.
````

###

Quarto, which is based on the Markdown language, has a variety of [formatting options](https://quarto.org/docs/authoring/markdown-basics.html).

Examples of Quarto formatting options include:

````
**bold** *italic* ~~strikeout~~ `code`

superscript^2^ subscript~2~

[underline]{.underline} [small caps]{.smallcaps}
````

<!-- Can we make the format of the above look like the below? Like put them both in boxes? Right now, only the raw text is in a box, because of the four backticks. -->

These look like:

**bold** *italic* ~~strikeout~~ `code`

superscript^2^ subscript~2~

[underline]{.underline} [small caps]{.smallcaps}

### Exercise 12

At the bottom of `about.qmd`, let's create text which is also a hyperlink to a website. Enclose the text "Kane's Free Data Science Bootcamp" in brackets `[]`. Then, without a space intervening, add parentheses `()` which enclose a link to the relevant website: `https://bootcamp.davidkane.info`.

It should look like:

````
[Kane's Free Data Science Bootcamp](https://bootcamp.davidkane.info)
````

`Cmd/Ctrl + Shift + K`. (Recall that rendering automatically saves the file first.)

In the R Terminal, run:

````
show_file("about.qmd", pattern = "Bootcamp")
````

CP/CR.


```{r the-website-12}
question_text(NULL,
    answer(NULL, correct = TRUE),
    allow_retry = TRUE,
    try_again_button = "Edit Answer",
    incorrect = NULL,
    rows = 3)
```

###

````
[Kane's Free Data Science Bootcamp](https://bootcamp.davidkane.info)
````

###

You only need a few [formatting tricks](https://quarto.org/docs/authoring/markdown-basics.html) for working with Quarto files, but you will use those tricks repeatedly.

### Exercise 13

Push all the changes to GitHub, and issue the `quarto publish gh-pages` command in the Terminal. You need to hit `Enter` at least once. CP/CR.

```{r the-website-13}
question_text(NULL,
	answer(NULL, correct = TRUE),
	allow_retry = TRUE,
	try_again_button = "Edit Answer",
	incorrect = NULL,
	rows = 3)
```

### 

Troubleshooting Quarto documents can be challenging because you are no longer in an interactive R environment. You will need to learn some new tricks. Additionally, an error could be due to issues with the Quarto document itself or due to the R code in the Quarto document.

If the errors are due to the R code in the document, the first thing you should always try is to recreate the problem in an interactive session. Render the QMD. If you’re lucky, that will recreate the problem, and you can figure out what’s going on interactively.

###

Recall from the Websites 1 tutorial what the publish command did behind the scenes: it rendered the site into a temporary worktree, committed the result to the `gh-pages` branch, and pushed. Run `git log --oneline gh-pages` any time to watch the "Built site for gh-pages" commits stack up --- every publish is just one more commit on the deployment branch.

### Exercise 14

Let's create new pages for your website, accessible via the navigation bar at the top.

Go to `File -> New File...` and select `Quarto Document`. Title it "Sources". Save the file as `sources.qmd`. 

### 

In the Terminal, run `ls *qmd`. CP/CR.

```{r the-website-14}
question_text(NULL,
    answer(NULL, correct = TRUE),
    allow_retry = TRUE,
    try_again_button = "Edit Answer",
    incorrect = NULL,
    rows = 3)
```

### 

````
websites-2 $ ls *qmd
about.qmd       index.qmd       sources.qmd
websites-2 $ 
````

You should see `index.qmd`, `about.qmd`, and `sources.qmd` in the output. If not, make sure you are in the `websites-2` directory. The `*qmd` regular expression selects only files with names which end with the letters `qmd`.

### Exercise 15

In the `sources.qmd` file, below the YAML header, add the text `An overview of sources used in my project.` Save the file.

In the R Terminal, run:

````
show_file("sources.qmd", pattern = "overview")
````

CP/CR.

```{r the-website-15}
question_text(NULL,
    answer(NULL, correct = TRUE),
    allow_retry = TRUE,
    try_again_button = "Edit Answer",
    incorrect = NULL,
    rows = 3)
```

###

````
An overview of sources used in my project.
````

###

Keep in mind that your browser probably has two tabs which show your website. First, the GitHub Pages version, which has not changed since you last hit `quarto publish gh-pages`. The only way to affect the public website is to publish to it. Second, the `localhost` version, which is what you have been changing every time that you render `index.qmd`.

### Exercise 16

Add the following sentence to `sources.qmd`: 

````
These are my sources. Here are some challenges. Quarto websites are awesome.
````

In the R Terminal, run:

````
show_file("sources.qmd")
````

CP/CR.

```{r the-website-16}
question_text(NULL,
    answer(NULL, correct = TRUE),
    allow_retry = TRUE,
    try_again_button = "Edit Answer",
    incorrect = NULL,
    rows = 3)
```

### 

````
---
title: "Sources"
format: html
---

An overview of sources used in my project.

These are my sources. Here are some challenges. Quarto websites are awesome.
````

###

A website, when you first look closely, may appear overwhelming in its complexity. But it is usually created from a small collection of text files, each of them quite simple.

You may be directed to the `localhost` version of your website, showing the "Sources" page. You have rendered the QMD into HTML. But notice that there is no way to navigate to this page from the website menu — it still needs to be added to `_quarto.yml`.

### Exercise 17

Open the `_quarto.yml` file. You should see a line that says `navbar`. Below this line you should see the line `left`. Below this is the code that creates the header links for the navigation bar on your site. It should look like:

```
website:
  title: "Websites 2"
  navbar:
    left:
      - href: index.qmd
        text: Home
      - about.qmd
```
    

### 

To add `sources.qmd` as a new page which will appear on the menu, we need to edit `_quarto.yml` to look like this:

```
website:
  title: "Websites 2"
  navbar:
    left:
      - href: index.qmd
        text: Home
      - about.qmd
      - href: sources.qmd
        text: Sources
```

Note the two additional lines. 

The `href` line tells Quarto to create an HTML page by rendering `sources.qmd` and to include that new page in the website. 

The `text` line tells Quarto to format the link in the **nav**igation tool**bar** as `Sources`. This is the same process by which the home page, created from `index.qmd`, has a link labeled `Home`.

Since `- about.qmd` appears without either the `href` or `text` argument, Quarto assumes that you want the link to be the same as the title of the QMD. 

### 

In the R Terminal, run:

````
show_file("_quarto.yml")
````

CP/CR.

```{r the-website-17}
question_text(NULL,
    answer(NULL, correct = TRUE),
    allow_retry = TRUE,
    try_again_button = "Edit Answer",
    incorrect = NULL,
    rows = 8)
```

### 

````
project:
  type: website

website:
  title: "Websites 2"
  navbar:
    left:
      - href: index.qmd
        text: Home
      - about.qmd
      - href: sources.qmd
        text: Sources

format:
  html:
    theme: cosmo
    css: styles.css
    toc: true
````

###

**YAML files are *very* tricky!** Tabs and white spaces matter. The code `left` before the navbar items puts the navigation bar on the left side of your site. This can be changed to `right`.

### Exercise 18

Run `quarto preview` from the Terminal. (You may need to first use `Ctrl + c` to end whatever program is currently running.) You will need to hit `Enter` once. Check out your new project! See how "Sources" now appears on the navigation toolbar. Click on "Sources" and see how the text you added to `sources.qmd` now appears on the web.

Run `ls _site/*html` in the Terminal. CP/CR.

```{r the-website-18}
question_text(NULL,
    answer(NULL, correct = TRUE),
    allow_retry = TRUE,
    try_again_button = "Edit Answer",
    incorrect = NULL,
    rows = 3)
```

### 

````
websites-2 $ ls _site/*html
_site/about.html        _site/sources.html
_site/index.html
websites-2 $ 
````

You should see `sources.html`. Quarto websites, like many similar frameworks, place all the pages which appear on the web into a single directory, like `_site`, and then move that directory onto a hosting service like GitHub Pages.

### Exercise 19

In the `_quarto.yml` file, delete the two lines of code that create the link and header for the "Sources" page.

In the Terminal, stop the preview with `Ctrl + c`. Then, use `rm` to remove `sources.qmd`. Run `ls *qmd` in the Terminal. CP/CR.

```{r the-website-19}
question_text(NULL,
    answer(NULL, correct = TRUE),
    allow_retry = TRUE,
    try_again_button = "Edit Answer",
    incorrect = NULL,
    rows = 3)
```

### 

There should not be a `sources.qmd` file. We are practicing these skills to make you more comfortable building up and tearing down the parts of a project. You need to build more than one house to get good at house-building!

### Exercise 20

Make two more empty Quarto documents, in the same way as you did above, with titles `"Source 1"` and `"Source 2"`.  

You should save them both (so they're not `untitled-1` and `untitled-2`). Name them `source-1.qmd` and `source-2.qmd`. 

In the Terminal, run the command `head source-1.qmd source-2.qmd`. CP/CR.

```{r the-websites-20}
question_text(NULL,
    answer(NULL, correct = TRUE),
    allow_retry = TRUE,
    try_again_button = "Edit Answer",
    incorrect = NULL,
    rows = 3)
```

### 

````
websites-2 $ head source-1.qmd source-2.qmd 
==> source-1.qmd <==
---
title: "Source 1"
format: html
---


==> source-2.qmd <==
---
title: "Source 2"
format: html
---

websites-2 $ 
````

In many Terminal commands, you can provide more than one file as the target of the command. In this case, `head` will be applied to `source-1.qmd` and then `source-2.qmd` sequentially.

### Exercise 21

Let's create a drop-down menu called "Sources" through which one could visit either the page for "Source 1" or "Source 2" from the top menu.

We will be changing YAML settings to enable this, so pay very close attention.

The Quarto [documentation](https://quarto.org/) provides an [excellent overview](https://quarto.org/docs/websites/website-navigation.html) of approaches to website navigation.

Change `_quarto.yml` so that the `navbar` section looks like this.

````
  navbar:
    left:
      - href: index.qmd
        text: Home
      - text: Sources
        menu:
          - href: source-1.qmd
            text: Source 1
          - href: source-2.qmd
            text: Source 2 
      - about.qmd
````        

In the R Terminal, run:

````
show_file("_quarto.yml")
````

CP/CR.

```{r the-websites-21}
question_text(NULL,
    answer(NULL, correct = TRUE),
    allow_retry = TRUE,
    try_again_button = "Edit Answer",
    incorrect = NULL,
    rows = 10)
```

### 

````
project:
  type: website

website:
  title: "Websites 2"
  navbar:
    left:
      - href: index.qmd
        text: Home
      - text: Sources
        menu:
          - href: source-1.qmd
            text: Source 1
          - href: source-2.qmd
            text: Source 2 
      - about.qmd

format:
  html:
    theme: cosmo
    css: styles.css
    toc: true
````

###

Run `quarto preview` to see the changes.

### Exercise 22

Go to the Source Control pane. Commit and push your new changes to GitHub. Include a descriptive commit message, e.g. "Added source pages".

From the Terminal, run `git log -n 1`. CP/CR. 

```{r the-websites-22}
question_text(NULL,
	answer(NULL, correct = TRUE),
	allow_retry = TRUE,
	try_again_button = "Edit Answer",
	incorrect = NULL,
	rows = 3)
```

### 

````
websites-2 $ git log -n 1
commit f0973447485da8bb8e8e36f65e39b5640519dccc (HEAD -> main, origin/main, origin/HEAD)
Author: David Kane <dave.kane@gmail.com>
Date:   Mon Mar 17 18:03:23 2025 -0400

    Added source pages
websites-2 $ 
````

Type `quarto publish` in the Terminal and hit "Enter." Note how you did not need to specify `gh-pages`. Quarto is smart enough to know that, since GitHub Pages is what you used last time, it is probably what you want now.  

Explore the use of the "Sources" menu on your website.

###

Your home page works, but there is a problem hiding in it. Every time you render the site, *all* of the billboard wrangling code in `index.qmd` runs again --- the date math, the loop, the pivot, the join. That is wasteful, especially for code that takes a while. Over the next few exercises we will move that work into scripts and save the results, so the website only has to display a finished image.

The first tool we need is the RDS format, which lets us save any R object to a file. When we read from an RDS, we get the object back without re-running the code that created it.

### Exercise 23

Close your R Terminal and open a new one to start fresh. Use `File -> New File...` to create a new file. Save it as `process_data.R`.

In this script, add `library(tidyverse)` and then, after a blank line, the billboard wrangling code from `index.qmd` --- everything *except* the final `ggplot()` pipe --- ending by assigning the result to an object named `top_songs`. Save the file.

In the Terminal, run `cat process_data.R`. CP/CR.

```{r the-websites-23}
question_text(NULL,
	answer(NULL, correct = TRUE),
	allow_retry = TRUE,
	try_again_button = "Edit Answer",
	incorrect = NULL,
	rows = 10)
```

###

````
websites-2 $ cat process_data.R
library(tidyverse)

billboard <- billboard |>
  mutate(date.entered = as.Date(date.entered),
         week_offset = as.numeric(difftime(date.entered, as.Date("2000-01-01"), units = "weeks")))

jul_week <- 26

billboard_jul <- billboard |>
  filter(date.entered <= as.Date("2000-07-01")) |>
  mutate(wk_idx = jul_week - week_offset,
         wk_idx = if_else(wk_idx >= 1 & wk_idx <= 76, wk_idx, NA_real_),
         jul_rank = NA_real_)

for (i in seq_len(nrow(billboard_jul))) {
  wk_idx <- billboard_jul$wk_idx[i]
  if (!is.na(wk_idx)) {
    col_name <- paste0("wk", wk_idx)
    billboard_jul$jul_rank[i] <- as.numeric(billboard_jul[[col_name]][i])
  }
}

top_5_songs <- billboard_jul |>
  filter(!is.na(jul_rank)) |>
  arrange(jul_rank) |>
  slice(1:5) |>
  pull(track)

billboard_filtered <- billboard |>
  filter(track %in% top_5_songs)

top_songs <- billboard_filtered |>
  select(artist, track, wk1:wk76) |>
  pivot_longer(cols = starts_with("wk"),
               names_to = "week_num",
               values_to = "rank") |>
  mutate(week_num = as.numeric(gsub("wk", "", week_num))) |>
  left_join(billboard_filtered |> select(track, week_offset), by = "track") |>
  mutate(week_num = week_num + week_offset) |>
  filter(week_num >= 0 & week_num <= 52) |>
  filter(!is.na(rank)) |>
  mutate(song_artist = paste0(track, " (", artist, ")"))
websites-2 $
````

Because this is a script, not a Quarto document, there are no code chunks and you do not need `suppressPackageStartupMessages()`. We put `library(tidyverse)` at the top so the script runs correctly even in a freshly restarted R session.

### Exercise 24

"Source" the entire script with echo by clicking the dropdown arrow next to the Run button (▷) at the top right of the editor. This feeds every line of the script to the R Terminal. CP/CR.

```{r the-websites-24}
question_text(NULL,
	answer(NULL, correct = TRUE),
	allow_retry = TRUE,
	try_again_button = "Edit Answer",
	incorrect = NULL,
	rows = 10)
```

###

Your answer should look something like:

````
> source("/workspaces/websites-2/process_data.R", echo = TRUE)

> library(tidyverse)
── Attaching core tidyverse packages ──────────────────────────────── tidyverse 2.0.0 ──
✔ dplyr     1.1.4     ✔ readr     2.1.5
✔ forcats   1.0.0     ✔ stringr   1.5.1
✔ ggplot2   3.5.1     ✔ tibble    3.2.1
✔ lubridate 1.9.4     ✔ tidyr     1.3.1
✔ purrr     1.0.4
── Conflicts ────────────────────────────────────────────────── tidyverse_conflicts() ──
✖ dplyr::filter() masks stats::filter()
✖ dplyr::lag()    masks stats::lag()
ℹ Use the conflicted package to force all conflicts to become errors

> billboard <- billboard |>
+   mutate(date.entered = as.Date(date.entered), .... [TRUNCATED]
>
````

Notice that the final line of the script assigns its result to `top_songs` rather than printing it, so no tibble appears. Assignment does not produce output.

### Exercise 25

From the R Terminal, run `ls()`. CP/CR.

```{r the-websites-25}
question_text(NULL,
	answer(NULL, correct = TRUE),
	allow_retry = TRUE,
	try_again_button = "Edit Answer",
	incorrect = NULL,
	rows = 3)
```

###

````
> ls()
[1] "billboard"          "billboard_filtered" "billboard_jul"
[4] "col_name"           "i"                  "jul_week"
[7] "top_5_songs"        "top_songs"          "wk_idx"
````

The script created several objects, including the one we care about, `top_songs`. The others are intermediate steps and loop variables --- real wrangling code often leaves a trail of them behind. Only `top_songs` is worth keeping.

### Exercise 26

Restart your R session by closing your R Terminal and opening a new one. From the R Terminal, run `ls()`. CP/CR.

```{r the-websites-26}
question_text(NULL,
	answer(NULL, correct = TRUE),
	allow_retry = TRUE,
	try_again_button = "Edit Answer",
	incorrect = NULL,
	rows = 3)
```

###

````
> ls()
character(0)
````

Every object vanished when R restarted. And that is fine! We still have `process_data.R`, which can recreate `top_songs` whenever we want. The code is the source of truth; the objects in memory are disposable.

### Exercise 27

Source `process_data.R` again to recreate `top_songs` (click the Run button ▷). Then, from the R Terminal, run `write_rds(top_songs, file = "clean_data.rds")`. [`write_rds()`](https://readr.tidyverse.org/reference/read_rds.html) comes from the **readr** package and saves an R object to a file; it produces no output.

In the Terminal, run `ls clean*`. CP/CR.

```{r the-websites-27}
question_text(NULL,
	answer(NULL, correct = TRUE),
	allow_retry = TRUE,
	try_again_button = "Edit Answer",
	incorrect = NULL,
	rows = 3)
```

###

````
websites-2 $ ls clean*
clean_data.rds
websites-2 $
````

`clean*` is a regular expression that matches any file beginning with `clean`. There are now three things associated with our data: the script `process_data.R` that creates it, the in-memory object `top_songs` that disappears when R restarts, and the file `clean_data.rds` that persists on disk.

### Exercise 28

So that the script itself saves its result, open `process_data.R` and, at the bottom after a blank line, add:

````
write_rds(top_songs, file = "clean_data.rds")
````

In the Terminal, run `tail -n 3 process_data.R`. CP/CR.

```{r the-websites-28}
question_text(NULL,
	answer(NULL, correct = TRUE),
	allow_retry = TRUE,
	try_again_button = "Edit Answer",
	incorrect = NULL,
	rows = 3)
```

###

````
websites-2 $ tail -n 3 process_data.R
  mutate(song_artist = paste0(track, " (", artist, ")"))

write_rds(top_songs, file = "clean_data.rds")
websites-2 $
````

This is a common pattern in larger projects: each script ends by saving an object that later scripts read in. Run `process_data.R` once, and `clean_data.rds` is ready for everything downstream.

### Exercise 29

Close your R Terminal and open a new one. Run `library(tidyverse)`, then run `read_rds("clean_data.rds")`. CP/CR.

```{r the-websites-29}
question_text(NULL,
	answer(NULL, correct = TRUE),
	allow_retry = TRUE,
	try_again_button = "Edit Answer",
	incorrect = NULL,
	rows = 10)
```

###

````
> read_rds("clean_data.rds")
# A tibble: 158 × 6
   artist  track     week_num  rank week_offset song_artist
   <chr>   <chr>        <dbl> <dbl>       <dbl> <chr>
 1 Aaliyah Try Again       12    59          11 Try Again (Aaliyah)
 2 Aaliyah Try Again       13    53          11 Try Again (Aaliyah)
 3 Aaliyah Try Again       14    38          11 Try Again (Aaliyah)
 4 Aaliyah Try Again       15    28          11 Try Again (Aaliyah)
 5 Aaliyah Try Again       16    21          11 Try Again (Aaliyah)
 6 Aaliyah Try Again       17    18          11 Try Again (Aaliyah)
 7 Aaliyah Try Again       18    16          11 Try Again (Aaliyah)
 8 Aaliyah Try Again       19    14          11 Try Again (Aaliyah)
 9 Aaliyah Try Again       20    12          11 Try Again (Aaliyah)
10 Aaliyah Try Again       21    10          11 Try Again (Aaliyah)
# ℹ 148 more rows
````

`read_rds()` returns whatever object was saved --- here, the `top_songs` tibble, ready to plot. We never had to re-run the wrangling.

### Exercise 30

Now we do the same thing for the plot. Use `File -> New File...` to create `make_plot.R`. Add `library(tidyverse)`, read the saved tibble, build the billboard plot (the `ggplot()` pipe from `index.qmd`) assigned to an object named `billboard_plot`, and save it with `ggsave()`. Save the file.

In the Terminal, run `cat make_plot.R`. CP/CR.

```{r the-website-30}
question_text(NULL,
	answer(NULL, correct = TRUE),
	allow_retry = TRUE,
	try_again_button = "Edit Answer",
	incorrect = NULL,
	rows = 10)
```

###

````
websites-2 $ cat make_plot.R
library(tidyverse)

x <- read_rds("clean_data.rds")

month_breaks <- seq(0, 48, by = 4)
month_labels <- c("Jan", "Feb", "Mar", "Apr", "May", "Jun",
                  "Jul", "Aug", "Sep", "Oct", "Nov", "Dec", "Jan")

billboard_plot <- x |>
  ggplot(aes(x = week_num, y = rank, color = song_artist)) +
  geom_line(linewidth = 1.2, alpha = 0.8) +
  geom_point(size = 2) +
  scale_y_reverse(limits = c(100, 1)) +
  scale_x_continuous(breaks = month_breaks, labels = month_labels) +
  scale_color_manual(values = c("#FF6B6B", "#4ECDC4", "#45B7D1", "#96CEB4", "#FFEEAD")) +
  labs(title = "Billboard Top 100: Top 5 Songs in Jul 2000",
       subtitle = "Tracking rank changes from Jan to Dec 2000",
       x = "Month",
       y = "Chart Position",
       color = "Song (Artist)") +
  theme_minimal() +
  theme(legend.position = "bottom")

ggsave("billboard.png", billboard_plot)
websites-2 $
````

The fact that the tibble was called `top_songs` in `process_data.R` is irrelevant. `write_rds()` saves the object, not its name, so we are free to call it `x` here.

### Exercise 31

Source `make_plot.R` (click the Run button ▷). In the Terminal, run `ls *.png`. CP/CR.

```{r the-website-31}
question_text(NULL,
	answer(NULL, correct = TRUE),
	allow_retry = TRUE,
	try_again_button = "Edit Answer",
	incorrect = NULL,
	rows = 3)
```

###

````
websites-2 $ ls *.png
billboard.png
websites-2 $
````

Just like RDS saves a tibble, a PNG saves a graphic. `ggsave()` writes the most recent plot (or the one you name) to an image file, so we avoid re-drawing it every time the site renders.

### Exercise 32

Now make `index.qmd` simply display the finished image. In its setup code chunk, add `library(knitr)`. In the second code chunk, delete all of the billboard code and replace it with `include_graphics("billboard.png")`. Save the file.

In the R Terminal, run `show_file("index.qmd")`. CP/CR.

```{r the-website-32}
question_text(NULL,
	answer(NULL, correct = TRUE),
	allow_retry = TRUE,
	try_again_button = "Edit Answer",
	incorrect = NULL,
	rows = 10)
```

###

<pre><code>---
title: "Billboard Graphic"
execute:
  echo: false
---


<pre><code>```{r}
suppressPackageStartupMessages(library(tidyverse))
library(knitr)
```</code></pre>

<pre><code>```{r}
include_graphics("billboard.png")
```</code></pre>
</code></pre>

###

There was nothing wrong with keeping the plotting code in `index.qmd`. The trouble is that it re-runs on every render. By moving the work into scripts and displaying only the saved image, the site renders almost instantly --- no matter how slow the original analysis was.

### Exercise 33

From the Terminal, run `quarto publish gh-pages`. You will need to hit `Enter` at least once. Then commit and push all the files, with a message like `"Use saved plot on home page"`.

From the Terminal, run `git show --name-status HEAD`. CP/CR.

```{r the-website-33}
question_text(NULL,
	answer(NULL, correct = TRUE),
	allow_retry = TRUE,
	try_again_button = "Edit Answer",
	incorrect = NULL,
	rows = 8)
```

###

````
websites-2 $ git show --name-status HEAD
commit 5ff52befc37ba1a3ac87dac781f48d6c6251c225 (HEAD -> main, origin/main, origin/HEAD)
Author: David Kane <dave.kane@gmail.com>
Date:   Mon Mar 17 20:08:52 2025 -0400

    Use saved plot on home page

A       billboard.png
A       clean_data.rds
M       index.qmd
A       make_plot.R
A       process_data.R
websites-2 $
````

This is the workflow professionals use. First, get something working all in one place. Second, split the slow parts into scripts --- `process_data.R` cleans the data and saves `clean_data.rds`, `make_plot.R` reads that and saves `billboard.png` --- so that `index.qmd` only has to show the image. In a larger project, these files often live in dedicated `code/`, `data/`, and `images/` directories. Separating data cleaning, visualization, and reporting makes pipelines faster to iterate on and easier to debug.

### Exercise 34

Navigate to your `websites-2` GitHub repo. Copy and paste the repository file listing.

```{r the-website-34}
question_text(NULL,
	answer(NULL, correct = TRUE),
	allow_retry = TRUE,
	try_again_button = "Edit Answer",
	incorrect = NULL,
	rows = 8)
```

###

Your file listing should look something like:

````
.gitignore      initial version           2 days ago
_quarto.yml     Added source pages        2 hours ago
about.qmd       Added plot to home page   3 hours ago
billboard.png   Use saved plot on home... 1 hour ago
clean_data.rds  Use saved plot on home... 1 hour ago
index.qmd       Use saved plot on home... 1 hour ago
make_plot.R     Use saved plot on home... 1 hour ago
process_data.R  Use saved plot on home... 1 hour ago
source-1.qmd    Added source pages        2 hours ago
source-2.qmd    Added source pages        2 hours ago
styles.css      initial version           2 days ago
````

Once you have confirmed your files are on GitHub, delete your Codespace. Click the green **Code** button on the repo page, go to the **Codespaces** tab, click **...** next to your Codespace, and select **Delete**.

###

When you ran `quarto publish gh-pages` for `website-1` and `websites-2`, Quarto was doing something behind the scenes worth understanding. It created (or updated) a separate `gh-pages` branch in your repo containing only the rendered HTML files from `_site`. Your `main` branch stays clean with just the source files. This is exactly why you added `_site` to `.gitignore` — the rendered files do live on GitHub, but in a different branch that GitHub Pages serves automatically. If you visit your repo on GitHub and click the branch dropdown, you will see both `main` and `gh-pages`.

## Summary
### 

This tutorial practiced the process of creating and organizing the [Quarto websites](https://quarto.org/docs/websites/) using the [Quarto CLI](https://quarto.org/docs/get-started/) (command line interface). You learned some new skills, such as how to add a visualization, edit text, and create new pages. You also learned how to save the results of your data-manipulation code in an RDS file and how to save your plots as PNG files. Some material is from [*R for Data Science (2e)*](https://r4ds.hadley.nz/) by Hadley Wickham, Mine Çetinkaya-Rundel, and Garrett Grolemund.

```{r download-answers, child = system.file("child_documents/download_answers.Rmd", package = "tutorial.helpers")}
```
